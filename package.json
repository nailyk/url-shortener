{
  "scripts": {
    "dev": "docker-compose up -d redis postgres && concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "test": "npm --prefix server run test",
    "test:e2e": "npm run test --prefix playwright",
    "test:e2e:ui": "npm run test:ui --prefix playwright",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky install",
    "clean:db": "docker-compose stop postgres && docker-compose rm -f postgres && docker volume rm url-shortener_postgres-data"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.5.3"
  },
  "lint-staged": {
    "*.{js,ts,tsx,css,json,md}": "prettier --write"
  }
}
